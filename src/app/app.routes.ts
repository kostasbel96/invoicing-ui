import { Routes } from '@angular/router';
import { CustomerForm } from './components/customer/customer-form/customer-form';

export const routes: Routes = [
  { path: 'customers', component: CustomerForm }
];
